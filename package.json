{
  "name": "vfile-read",
  "version": "1.3.2",
  "description": "Read file or directory into vfile.",
  "main": "lib/index.js",
  "author": "Paul Zimmer",
  "license": "MIT",
  "scripts": {
    "xo": "xo --fix",
    "tap": "tap --100 -Rspec test",
    "test": "npm run xo && npm run tap",
    "report": "nyc report --reporter=text-lcov | coveralls"
  },
  "keywords": [
    "virtual",
    "file",
    "directory",
    "readFile",
    "readdir",
    "path",
    "file-path"
  ],
  "repository": "mrzmmr/vfile-read",
  "bugs": "https://github.com/mrzmmr/vfile-read/issues",
  "dependencies": {
    "bail": "^1.0.3",
    "vfile": "^2.3.0"
  },
  "devDependencies": {
    "coveralls": "^3.0.2",
    "clone": "^2.1.1",
    "tap": "^12.7.0",
    "xo": "^0.24.0"
  }
}
